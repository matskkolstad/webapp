# LiggNett environment variables (example)
# Copy to .env and fill in real values for your environment.

DATABASE_URL=postgresql://liggnett:<password>@localhost:5432/liggnett?schema=public
JWT_SECRET=replace-with-a-long-random-secret
PORT=3000
